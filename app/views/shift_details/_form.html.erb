<%= form_for(@shift_detail) do |f| %>
  <% if @shift_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shift_detail.errors.count, "error") %> prohibited this shift_detail from being saved:</h2>

      <ul>
      <% @shift_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :shift_name %><br>
    <%= f.text_field :shift_name %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time %>
  </div>
  <div class="field">
    <%= f.label :end_time %><br>
    <%= f.datetime_select :end_time %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
